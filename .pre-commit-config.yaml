# Local commit-time secret scanning (#4).
# Install once per clone:  pre-commit install
# See the root README, "Secret scanning", for setup and bypass policy.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
